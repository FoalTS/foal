import { LocalStrategyService } from './local-strategy.service';

describe('LocalStrategyService', () => {

  let service: LocalStrategyService;

  it('should instantiate.', () => {
    service = new LocalStrategyService();
  });
});
