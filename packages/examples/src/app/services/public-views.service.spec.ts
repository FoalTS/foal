import { PublicViewsService } from './public-views.service';

describe('PublicViewsService', () => {

  let service: PublicViewsService;

  it('should instantiate.', () => {
    service = new PublicViewsService();
  });

  describe('when render(name: string, locals: ObjectType): Promise<string> is called', () => {

    it('should return the rendered template.', () => {

    });

  });

});
