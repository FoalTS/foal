import { expect } from 'chai';

import { Context, Hook, Route } from '../interfaces';
import { Service, ServiceManager } from './service-manager';
import { createEmptyContext } from '../testing';
import { ServiceControllerFactory } from './service-controller-factory';

xdescribe('ControllerFactory', () => {

  describe('when attachService is called', () => {

  });

});
