{
  "name": "@foal/swagger",
  "version": "5.3.0",
  "description": "Swagger UI for FoalTS",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "scripts": {
    "test": "rimraf lib && copy-cli \"./src/**.html\" lib && copy-cli \"./src/**.tpl.js\" lib && tsc -p tsconfig.json && mocha \"./lib/**/*.spec.js\"",
    "dev:test": "mocha --require ts-node/register --watch --extension ts \"./src/**/*.spec.ts\"",
    "build": "rimraf lib && copy-cli \"./src/**.html\" lib && copy-cli \"./src/**.tpl.js\" lib && tsc -p tsconfig-build.json",
    "prepublish": "npm run build"
  },
  "funding": {
    "url": "https://github.com/sponsors/LoicPoullain"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "keywords": [
    "FoalTS",
    "foal",
    "openapi",
    "swagger",
    "ui",
    "spec"
  ],
  "bugs": {
    "url": "https://github.com/FoalTS/foal/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/FoalTS/foal"
  },
  "homepage": "https://github.com/FoalTS",
  "contributors": [
    "Loïc Poullain"
  ],
  "license": "MIT",
  "files": [
    "lib/"
  ],
  "dependencies": {
    "@foal/core": "^5.3.0",
    "swagger-ui-dist": "~5.32.8"
  },
  "devDependencies": {
    "@types/mocha": "10.0.10",
    "@types/node": "24.13.2",
    "copy": "~0.3.2",
    "mocha": "~11.7.6",
    "rimraf": "~6.1.3",
    "ts-node": "~10.9.2",
    "typescript": "~5.9.3"
  }
}
